{
  "name": "cvnss4-actions",
  "version": "5.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "assemble": "node scripts/assemble-converter.mjs",
    "test": "npm run assemble && node test.mjs",
    "dev": "npm run assemble && npx wrangler dev",
    "deploy": "npm run assemble && npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
